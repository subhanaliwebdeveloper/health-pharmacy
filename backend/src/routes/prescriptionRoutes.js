import {Router} from 'express';import * as c from '../controllers/prescriptionController.js';import {protect} from '../middleware/authMiddleware.js';import {adminOnly} from '../middleware/adminMiddleware.js';import {upload} from '../middleware/uploadMiddleware.js';const r=Router();r.post('/',protect,upload.single('prescription'),c.create);r.get('/mine',protect,c.mine);r.get('/',protect,adminOnly,c.all);r.patch('/:id',protect,adminOnly,c.update);export default r;
