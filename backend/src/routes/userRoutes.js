import {Router} from 'express';import * as c from '../controllers/userController.js';import {protect} from '../middleware/authMiddleware.js';import {adminOnly} from '../middleware/adminMiddleware.js';const r=Router();r.get('/',protect,adminOnly,c.all);r.get('/:id',protect,adminOnly,c.one);r.get('/:id/orders',protect,adminOnly,c.ordersFor);r.put('/profile',protect,c.updateProfile);export default r;
