import React from "react";
import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Login(){const {login}=useAuth(),nav=useNavigate(),loc=useLocation(),[form,setForm]=useState({email:'',password:''}),[err,setErr]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);try{await login(form);nav(new URLSearchParams(loc.search).get('next')||'/')}catch(e){setErr(e.message)}finally{setBusy(false)}};return <div className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-head"><span className="brand-icon">✚</span><h1>Welcome Back!</h1><p>Login to your Health Pharmacy account</p></div>{err&&<div className="error">{err}</div>}<label>Email or Phone<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><button className="btn-primary full" disabled={busy}>{busy?'Logging in...':'Login'}</button><p className="center">Don't have an account? <Link to="/register">Sign up</Link></p></form></div>}
