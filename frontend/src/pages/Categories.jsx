import React from "react";
import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {getCategories} from '../services/productService';export default function Categories(){const [cats,setCats]=useState([]);useEffect(()=>{getCategories().then(setCats)},[]);return <div className="page"><div className="page-title"><span className="eyebrow">SHOP BY CATEGORY</span><h1>Health Categories</h1></div><div className="category-large">{cats.map(c=><Link to={`/products?category=${c.id}`} key={c.id}><span>💊</span><div><h3>{c.name}</h3><small>{c.product_count} products</small></div>→</Link>)}</div></div>}
