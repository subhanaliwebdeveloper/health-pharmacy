import React from "react";
import {useEffect,useState} from 'react';import {Navigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {request} from '../services/api';export default function Profile(){const {user}=useAuth(),[f,setF]=useState({name:'',phone:'',address:'',city:''}),[msg,setMsg]=useState('');useEffect(()=>{if(user)setF({name:user.name||'',phone:user.phone||'',address:user.address||'',city:user.city||''})},[user]);if(!user)return <Navigate to="/login"/>;const save=async e=>{e.preventDefault();try{await request('/users/profile',{method:'PUT',body:JSON.stringify(f)});setMsg('Profile updated')}catch(e){setMsg(e.message)}};return <div className="page narrow"><div className="page-title"><span className="eyebrow">MY ACCOUNT</span><h1>Profile</h1></div><form className="form-card" onSubmit={save}>{['name','phone','address','city'].map(k=><label key={k}>{k[0].toUpperCase()+k.slice(1)}<input value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<button className="btn-primary">Save Changes</button>{msg&&<div className="notice">{msg}</div>}</form></div>}
