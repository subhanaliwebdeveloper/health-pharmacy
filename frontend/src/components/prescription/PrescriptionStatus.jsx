import React from "react";
export default function PrescriptionStatus({items=[]}){return <div className="form-card"><h3>Prescription History</h3>{!items.length?<p className="muted">No prescriptions uploaded yet.</p>:items.map(p=><div className="rx-row" key={p.id}><div><b>Prescription #{p.id}</b><small>{new Date(p.created_at).toLocaleString()}</small></div><span className={`status ${p.status}`}>{p.status}</span></div>)}</div>}
