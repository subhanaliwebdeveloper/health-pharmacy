import React from "react";
import {useEffect,useState} from 'react';import {request} from '../services/api';export default function Payments(){const [m,setM]=useState([]);useEffect(()=>{request('/payments/methods').then(setM)},[]);return <><div className="page-head"><div><small>CHECKOUT</small><h1>Payments</h1></div></div><section className="panel"><h2>Payment Methods</h2>{m.map(x=><div className="method" key={x.id}><b>{x.name}</b><span className={x.enabled?'pill delivered':'pill cancelled'}>{x.enabled?'Enabled':'Disabled'}</span><small>{x.note||''}</small></div>)}</section></>}
