import React from "react";
import { useState } from 'react'; import { useAuth } from '../context/AuthContext'; export default function Login() { const { login } = useAuth(), [f, setF] = useState({ email: 'admin@healthpharmacy.pk', password: 'Admin@123' }), [err, setErr] = useState(''); return <div className="admin-login"><form onSubmit={async e => { e.preventDefault(); try { await login(f) } catch (e) { setErr(e.message) } }}><div className="admin-logo">✚</div><h1>Admin Portal</h1><p>Manage Health Pharmacy</p>{err && <div className="err">{err}</div>}<input type="email" value={f.email} onChange={e => setF({ ...f, email: e.target.value })} placeholder="Admin email" /><input type="password" value={f.password} onChange={e => setF({ ...f, password: e.target.value })} placeholder="Password" /><button>Sign In</button></form></div> }
