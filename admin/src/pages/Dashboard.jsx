import React from "react";
import {useEffect,useState} from 'react';import {request} from '../services/api';import StatsCard from '../components/StatsCard';import DataTable from '../components/DataTable';import {statusLabel} from '../utils/orderStatus';export default function Dashboard(){const [p,setP]=useState([]),[o,setO]=useState([]),[u,setU]=useState([]),[rx,setRx]=useState([]);const load=()=>Promise.all([request('/products/admin/all'),request('/orders'),request('/users'),request('/prescriptions')]).then(([a,b,c,d])=>{setP(a);setO(b);setU(c);setRx(d)});useEffect(()=>{load()},[]);const sales=o.filter(x=>x.status!=='cancelled').reduce((s,x)=>s+Number(x.total),0);return <><div className="page-head"><div><small>OVERVIEW</small><h1>Dashboard</h1></div></div><div className="stats"><StatsCard title="Total Sales" value={`Rs. ${sales.toLocaleString()}`} icon="💰" note="All completed/active orders"/><StatsCard title="Orders" value={o.length} icon="🛍️" note="All customer orders"/><StatsCard title="Customers" value={u.filter(x=>x.role==='customer').length} icon="👥" note="Registered users"/><StatsCard title="Pending Prescriptions" value={rx.filter(x=>x.status==='pending').length} icon="📄" note="Needs review"/></div><section className="panel"><div className="panel-head"><h2>Recent Orders</h2></div><DataTable rows={o.slice(0,8)} columns={[{key:'order_number',label:'Order'},{key:'customer_name',label:'Customer'},{key:'total',label:'Total',render:r=>`Rs. ${Number(r.total).toLocaleString()}`},{key:'status',label:'Status',render:r=><span className={`pill ${r.status}`}>{statusLabel(r.status)}</span>},{key:'created_at',label:'Date',render:r=>new Date(r.created_at).toLocaleDateString()}]}/></section></>}
