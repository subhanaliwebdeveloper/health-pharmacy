import React from "react";
import {request,API} from '../services/api';export default function PrescriptionViewer({rows,onChange}){const update=async(id,status)=>{await request(`/prescriptions/${id}`,{method:'PATCH',body:JSON.stringify({status})});onChange?.()};return <div className="table-wrap"><table><thead><tr><th>Customer</th><th>Prescription</th><th>Status</th><th>Date</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td>{p.customer_name}<small>{p.email}</small></td><td><a className="view-link" target="_blank" rel="noreferrer" href={`${API.replace('/api','')}${p.image_url}`}>View Image</a></td><td><select className="status-select" value={p.status} onChange={e=>update(p.id,e.target.value)}><option>pending</option><option>approved</option><option>rejected</option></select></td><td>{new Date(p.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div>}
